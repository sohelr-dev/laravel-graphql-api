
# one type access ,one Queries has another function get data below
# type Query {
#   user: String @field(resolver:"Test@index")
# } 

# must only this method file name 

# type Query {
#   test: String
#   user: String
# } 
type UserType {
  id:Int
  name:String
  email:String!
  password:String!
}

# sub selection error asle user under id,email like 
# type Query {
#   test: String
#   user: [User!]! @all
# } 


type Query {
  user(id:Int): User @field(resolver: "User@Show")
  test: String
  users: [User!]! @all
} 

# type Query {
#   test: String
#   users: String
# } 

# ! this call mendetory field

type Mutation{
  createUser( name:String!, email:String!, password:String!):UserType!
    @field(resolver:"User@create")
    UpdateUser(id:Int!,name:String!, email:String!):UserType!
    @field(resolver:"User@update")
    deleteUser(id:Int!):UserType!
    @field(resolver:"User@delete")

}